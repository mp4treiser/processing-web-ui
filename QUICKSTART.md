# 🚀 Quick Start Guide

## Запуск через Docker (Самый простой способ)

```bash
# 1. Клонируйте репозиторий (если еще не сделали)
# git clone <your-repo>

# 2. Перейдите в директорию проекта
cd processing-web-ui

# 3. Запустите все сервисы одной командой
docker-compose up -d

# 4. Подождите ~30 секунд для инициализации БД

# 5. Откройте в браузере:
# - Frontend: http://localhost:3000
# - Backend API: http://localhost:8000
# - API Docs: http://localhost:8000/docs
```

## Вход в систему

Используйте тестовые учетные записи:

- **Менеджер**: `manager@test.com` / `manager123`
- **Бухгалтер**: `accountant@test.com` / `accountant123`
- **ФинДиректор**: `director@test.com` / `director123`

## Основной workflow

1. **Менеджер** создает новую заявку:
   - Заходит под `manager@test.com`
   - Нажимает "New Deal"
   - Заполняет форму с транзакциями
   - Нажимает "Submit for Calculation"

2. **Бухгалтер** рассчитывает маршруты:
   - Заходит под `accountant@test.com`
   - Переходит в "Calculation Queue"
   - Выбирает заявку
   - Выбирает маршруты для каждой транзакции
   - Нажимает "Calculate All"
   - Нажимает "Submit for Director Approval"

3. **ФинДиректор** утверждает:
   - Заходит под `director@test.com`
   - Переходит в "Approvals"
   - Просматривает экономику сделки
   - Нажимает "Approve" или "Reject"

4. **Менеджер** согласовывает с клиентом:
   - Видит утвержденную сумму
   - Нажимает "Client Approved - Await Payment"

5. **Бухгалтер** исполняет платежи:
   - После получения USDT от клиента
   - Отмечает каждую транзакцию как "Paid"

## Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Очистка (удалит БД!)
docker-compose down -v
```

## Troubleshooting

**Backend не запускается?**
```bash
docker-compose logs backend
# Проверьте, что PostgreSQL запущен
docker-compose ps
```

**Frontend не подключается к API?**
- Проверьте, что backend доступен на http://localhost:8000
- Откройте консоль браузера (F12) для проверки ошибок

**База данных не инициализирована?**
```bash
docker-compose exec backend alembic upgrade head
docker-compose exec backend python scripts/seed_data.py
```

